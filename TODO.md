# API Connection Plan - Progress Tracker

## Approved Plan Steps:
1. [ ] Edit package.json to add dev script for nodemon.
2. [ ] Create .env from example and fill DB creds (user provides).
3. [ ] Run `npm install` (deps include express, mysql2, knex, axios).
4. [ ] Run migrations: `npx knex migrate:latest`.
5. [ ] Run seeds: `npx knex seed:run`.
6. [ ] Test DB: `node src/test-db.js`.
7. [ ] Start server: `npm run dev`.
8. [ ] Test API endpoints (Postman or curl).

## Current Status: Waiting for plan approval & DB creds.

**Notes:** Local MySQL required. Axios ready for external APIs if needed.
